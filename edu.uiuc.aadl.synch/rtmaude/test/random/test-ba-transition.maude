set show timing off .

load ../../interpreter-random.maude

load module-ba-semantics



---------------------------------------------------
select TEST-BEHAVIOR-TRANSITION-SEMANTICS .

red execTrans(location(l1) complete(l1) trans(trans1) bcf) .

red execTrans(location(l1) complete(l1) trans(trans2) bcf) .

red execTrans(location(l1) complete(l1) trans(trans3) bcf) .


